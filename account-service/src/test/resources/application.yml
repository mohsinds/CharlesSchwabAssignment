spring:
  application:
    name: account-service
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:account-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

management:
  tracing:
    enabled: false
  otlp:
    tracing:
      export:
        enabled: false
